(jbuild_version 1)

(executables
 ((names (sample_exe1))
  (public_names (sample-exe1))
  (modules (sample_exe1))
  (libraries (sample-lib1))
  (package sample-exe)))

(executables
 ((names (sample_exe2))
  (public_names (sample-exe2))
  (modules (sample_exe2))
  (libraries (sample-lib1 sample-lib2))
  (package sample-exe)))
