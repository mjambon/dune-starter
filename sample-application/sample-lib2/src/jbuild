(jbuild_version 1)

(library
 ((name sample_lib2)   ; doesn't support '-' in the name
  (public_name sample-lib2)
  (wrapped false)
  (libraries (unix str sample-lib1))
  (synopsis "This is a short description of the library.")))
